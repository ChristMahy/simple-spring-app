<?xml version="1.0" encoding="UTF-8"?>
<project
        xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
>
    <modelVersion>4.0.0</modelVersion>

    <groupId>cmahy.webapp.taco.shop.adapter.repository</groupId>
    <artifactId>cassandra</artifactId>
    <version>DEV-SNAPSHOT</version>

    <packaging>jar</packaging>

    <parent>
        <groupId>cmahy</groupId>
        <artifactId>bom</artifactId>
        <version>DEV-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <properties>
        <cmahy.common>DEV-SNAPSHOT</cmahy.common>

        <cmahy.webapp.taco.shop.version>DEV-SNAPSHOT</cmahy.webapp.taco.shop.version>
        <cmahy.webapp.user.version>DEV-SNAPSHOT</cmahy.webapp.user.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>cmahy.webapp.taco.shop.kernel</groupId>
            <artifactId>application</artifactId>
            <version>${cmahy.webapp.taco.shop.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>cmahy.webapp.taco.shop.kernel</groupId>
            <artifactId>domain</artifactId>
            <version>${cmahy.webapp.taco.shop.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>cmahy.webapp.user.kernel</groupId>
            <artifactId>domain</artifactId>
            <version>${cmahy.webapp.user.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>cmahy.webapp.user.adapter.repository</groupId>
            <artifactId>cassandra</artifactId>
            <version>${cmahy.webapp.user.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-cassandra</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <dependency>
            <groupId>cmahy</groupId>
            <artifactId>common</artifactId>
            <version>${cmahy.common}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>taco-shop-adapter-repository-${project.artifactId}-${project.version}</finalName>

        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/cassandra/config/**</exclude>
                        <exclude>**/cassandra/entity/domain/**</exclude>
                        <exclude>**/cassandra/repository/cassandra/**</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <reporting>
        <outputDirectory>${project.build.directory}/${project.build.finalName}-reports</outputDirectory>
    </reporting>
</project>