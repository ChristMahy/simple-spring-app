package cmahy.springapp.reactive.service.zipper;

import org.springframework.core.io.buffer.DataBuffer;

public record ZipperAttachment(
    /*DataBuffer dataBuffer,
    DataBuffer.ByteBufferIterator iterator*/
) {}
